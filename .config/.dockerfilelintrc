rules:
  missing_tag: off # disabled becuase of https://github.com/replicatedhq/dockerfilelint/issues/187
  apt-get-update_require_install: off # disable because of false-positives when using playwright install-deps
  latest_tag: off # disable because sometimes it's necessary and handled by hadolint instead
